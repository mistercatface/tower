/** Spawn-only — stamps the belt + crate three-room puzzle template into `state.roomGraph`. */
export default { id: "puzzle_belt_crate", sandbox: { spawnLabel: "Belt + crate puzzle", puzzleTemplate: true, tags: ["rooms"] }, physics: { renderMode: "none" } };
