/** Spawn-only — corridor links live in `state.roomGraph`, not WorldProps. */
export default { id: "corridor", sandbox: { spawnLabel: "Corridor", roomLink: true, tags: ["rooms"] }, physics: { renderMode: "none" } };
