/** Spawn-only — room nodes are stamped on the grid as rectangular footprints, not WorldProps. */
export default { id: "grid_room_node", sandbox: { spawnLabel: "Room node", gridRoomNode: true }, physics: { renderMode: "none" } };
