/** Spawn-only catalog entry — power sources are stamped on `obstacleGrid.floorStore`, not WorldProps. */
export default { id: "floor_power_source", sandbox: { spawnLabel: "Power source", gridPassagePowerSource: true }, physics: { renderMode: "none" } };
