/** Spawn-only — railed elbow belt on `floorStore` + lateral `beltRail` edges. */
export default { id: "floor_belt_elbow_right_rails", sandbox: { spawnLabel: "Conveyor Elbow R (rails)", gridFloorBelt: true, floorBeltKind: "elbowRightRails" }, physics: { renderMode: "none" } };
