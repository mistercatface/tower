import { PROP_RENDER_MODE_NONE } from "../../../Core/engineEnums.js";
/** Spawn-only — elbow belt stamped on `floorStore`, not a WorldProp. */
export default { id: "floor_belt_elbow_left", sandbox: { spawnLabel: "Conveyor Elbow L", gridFloorBelt: true, floorBeltKind: "elbowLeft" }, physics: { renderMode: PROP_RENDER_MODE_NONE } };
