/** Spawn-only — elbow belt stamped on `floorStore`, not a WorldProp. */
export default { id: "floor_belt_elbow_left", sandbox: { spawnLabel: "Conveyor Elbow L", gridFloorBelt: true, floorBeltKind: "elbowLeft" }, physics: { renderMode: "none" } };
