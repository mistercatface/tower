/** Spawn-only catalog entry — belts are stamped on `obstacleGrid.floorStore`, not WorldProps. */
export default { id: "floor_belt", sandbox: { spawnLabel: "Conveyor", gridFloorBelt: true }, physics: { renderMode: "none" } };
