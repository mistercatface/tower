/** Spawn-only — railed belt stamped on `floorStore` + `beltRail` edges on lateral cell sides. */
export default { id: "floor_belt_rails", sandbox: { spawnLabel: "Conveyor (rails)", gridFloorBelt: true, floorBeltKind: "straightRails" }, physics: { renderMode: "none" } };
