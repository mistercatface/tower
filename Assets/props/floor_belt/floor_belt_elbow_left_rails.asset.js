/** Spawn-only — railed elbow belt on `floorStore` + lateral `beltRail` edges. */
export default { id: "floor_belt_elbow_left_rails", sandbox: { spawnLabel: "Conveyor Elbow L (rails)", gridFloorBelt: true, floorBeltKind: "elbowLeftRails" }, physics: { renderMode: "none" } };
