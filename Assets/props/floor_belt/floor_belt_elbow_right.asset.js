import { PROP_RENDER_MODE_NONE } from "../../../Core/engineEnums.js";
/** Spawn-only — elbow belt stamped on `floorStore`, not a WorldProp. */
export default { id: "floor_belt_elbow_right", sandbox: { spawnLabel: "Conveyor Elbow R", gridFloorBelt: true }, physics: { renderMode: PROP_RENDER_MODE_NONE } };
