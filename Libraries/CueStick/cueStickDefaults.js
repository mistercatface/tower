/** Default dimensions for a table-length lo-fi cue stick (world units). */
export const CUE_STICK_DEFAULTS = {
    hx: 72,
    hy: 1.15,
    height: 2.3,
    rollAngle: Math.PI / 2,
    tipGap: 0,
    pullScale: 0.55,
    maxPull: 50,
    strikeSpeed: 400,
    minStrikePull: 4,
};
