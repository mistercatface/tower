/**
 * Roguelike map graph + backdrop tuning — define per game in `Games/<id>/mapTopology.js`.
 *
 * @typedef {object} RoguelikeMapTopology
 * @property {number} numLayers
 * @property {number} layerSpacing
 * @property {number} xSpacing
 * @property {number} nodeJitter
 * @property {number} extraConnectionChance
 * @property {number} backdropMargin
 * @property {number} roomZoneRadius
 * @property {number} caFillChance
 * @property {number} caIterations
 * @property {number} nodeRoomSerializeRadius
 */
export {};
