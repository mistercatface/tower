/** @deprecated Import from `../skeleton/` — re-exported for backward compatibility. */
export {
    PHYSICS_BONES,
    JOINT_TO_RIG_PATH,
    RAGDOLL_CONSTRAINT_EDGES,
    PHYSICS_BONE_ALIASES,
    SEVER_LIMB_DEF,
    SEVER_TORSO_PEER,
    SEVER_STUMP_BONES,
    getRigPoint,
    applyRigDeltas,
    boneMapFromCharacterRig,
    resolvePhysicsBoneId,
} from "../skeleton/index.js";
