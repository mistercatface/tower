/** Shared explosion tuning — games may override via prop `explosion` config or game balance. */
export const LIBRARY_EXPLOSION_DEFAULTS = { defaultDamage: 5, barrelDamage: 5, wallBlastDamage: 5, playerMultipliers: [1, 0.5], enemyMultipliers: [1.6, 0.4] };
