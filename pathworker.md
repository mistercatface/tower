## REFINEMENT

Stop clearing path on click (sandbox only) — immediate UX win, uses existing session design.
One-shot worker replan — kill 14 ping-pongs.
Abstract-first + first-leg-only apply — incremental HPA behavior.
Persistent abstract graph on worker — scaling + kill main-thread bake.
Snapshot build off click path — kill main-thread lock on huge grids.
Leg advancement while walking — complete region-at-a-time model.
